#include "models.h"

#include "vertex.h"

#include <vector>
#include <fstream>
#include <string>


std::vector<renderer::vertex> assets::loadModel(const char* filepath) {
	std::ofstream file{ filepath };

	return {};
}
